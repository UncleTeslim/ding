{
  "name": "@ding/widget",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "test": "vitest run --environment jsdom",
    "check:size": "gzip-size dist/widget.js --raw --max-size 25000"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "esbuild": "^0.21.3",
    "jsdom": "^24.1.0",
    "typescript": "^5.4.5"
  }
}
